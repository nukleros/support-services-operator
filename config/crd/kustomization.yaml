# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/orchestration.support-services.nukleros.io_supportservices.yaml
- bases/certificates.support-services.nukleros.io_certmanagers.yaml
- bases/ingress.support-services.nukleros.io_externaldns.yaml
- bases/secrets.support-services.nukleros.io_externalsecrets.yaml
- bases/secrets.support-services.nukleros.io_reloaders.yaml
#+kubebuilder:scaffold:crdkustomizeresource

patchesStrategicMerge:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
#- patches/webhook_in_supportservices.yaml
#- patches/webhook_in_certmanagers.yaml
#- patches/webhook_in_externaldns.yaml
#- patches/webhook_in_externalsecrets.yaml
#- patches/webhook_in_reloaders.yaml
#+kubebuilder:scaffold:crdkustomizewebhookpatch

# [CERTMANAGER] To enable cert-manager, uncomment all the sections with [CERTMANAGER] prefix.
# patches here are for enabling the CA injection for each CRD
#- patches/cainjection_in_supportservices.yaml
#- patches/cainjection_in_certmanagers.yaml
#- patches/cainjection_in_externaldns.yaml
#- patches/cainjection_in_externalsecrets.yaml
#- patches/cainjection_in_reloaders.yaml
#+kubebuilder:scaffold:crdkustomizecainjectionpatch

# the following config is for teaching kustomize how to do kustomization for CRDs.
configurations:
- kustomizeconfig.yaml
